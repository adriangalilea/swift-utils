# TODO

- [ ] **Changelog - the full release history, in-app + version-jump aware (Adrian, 2026-07-27).** Not the feature board the about surface links today - the CHANGELOG itself, readable in-app: every version, newest first. STATE-AWARE: persist the last version the user actually RAN; after an update the surface leads with "you jumped N versions - here's what's new since <that one>", then marks current as seen (the same signal can badge the About row). THE SOURCE IS THE APPCAST: the Sparkle feed every app already ships is the one place per-version release notes already live - owned, remote (always complete, no bundle bloat), and the notes are HTML, which IS the rich-media path (images/video as remote URLs rendering in-app). Fetch + cache; offline shows the cache. Ships as its own product beside Colophon (a "What's new" row opening the history). The quality gate is upstream: the changelog renders exactly what the release pipeline writes into the feed, so the notes step must write for HUMANS.

- [ ] **Feedback - the built-in canvas for issues + feature requests (Adrian, 2026-07-27).** Every app gets an INLINE surface - no browser, no account wall: browse the app's board, vote, compose a bug report or feature request (markdown + image attach). Identity is a random install UUID in the Keychain, sent ONLY on feedback actions, never a passive beacon. The backend already exists (the studio board's HTTP API: list / submit / vote / upload; base URL + app key are the per-app config), so this is the Swift client, designed once: sits on Ink, the Colophon-class product beside it in Settings. Read surface first (browse + vote is the low-friction win), compose second; an optional "link this install to an account" hand-off dedups votes across devices.

- [ ] **keymap-overlay: WIP, parked (2026-07-27).** Only adopting apps publish manifests, so on any other frontmost app the chord shows nothing - worthless until coverage exists. The unlock to design before resuming: derive a card for ARBITRARY apps (walk the frontmost app's menu bar via accessibility, and/or its published App Intents) so ⌃⌘/ answers everywhere, with published manifests as the rich path. Not running anywhere meanwhile.

- [ ] **AppSettings scaffold (when a third app makes the shape obvious).** One drop-in Settings scene with the standard panes pre-wired - Shortcuts (KeymapGrid), General, About (ColophonPane) - so support surfaces exist in every app without per-app assembly. Module name must NOT be `Settings` (collides with SwiftUI.Settings). Colophon and Entitlement ship; this is the composition layer over them.
- [ ] **Entitlement gating affordances.** The gate ships (one boolean, sealed/dev construction, `EntitlementDevToggle`). Still unextracted, waiting on a second real paywall: the locked badge, the upgrade prompt, the paywall sheet shape.
