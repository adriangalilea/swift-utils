# TODO

- [ ] **Settings pattern (next product after Keymap ships end-to-end).** One Settings scaffold every app drops in: tabbed Settings scene with the standard panes pre-wired - Shortcuts (KeymapSettingsPane), General, and a CREDITS/SUPPORT pane (app version + what's-new link, tip jar / Ko-fi / Sponsors links, "made by adrian galilea" with site link) so support surfaces exist in every app without per-app work. Module name must NOT be `Settings` (collides with SwiftUI.Settings scene) - candidate: `AppSettings` or fold into the credits idea and name it for what it is.
- [ ] **Premium/entitlement pattern (critical before hollow ships paid).** The reusable shape of "this feature is paid": a feature-flag surface with LOCAL dev overrides (env var or UserDefaults toggle to force premium on/off in dev builds, never shippable), license check pluggable per app (garden commerce/license rows are the backend), gating UI affordances (locked badge, upgrade prompt). lore's Entitlement.swift and hollow's monetization gate are the two live inputs - extract when the second one lands, design the flag surface FIRST so dev testing is day one, not bolted on.
